let apiEndpoint

if (process.env.NODE_ENV === 'development') {
    apiEndpoint = 'https://api.starowl.social'
} else if (process.env.NODE_ENV === 'production') {
    apiEndpoint = 'http://localhost:5000'
}
